doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="/output.css")
    body( class="max-w-4xl mx-auto p-4 bg-black")
        h1(class="mt-16 text-5xl font-bold text-amber-700") MORE DETAILS
        img(src=`https://image.tmdb.org/t/p/w500${data.poster_path}`, alt="")   
        h1(class="text-amber-700")= data.title
        p(class="text-amber-700")= data.vote_average
        p(class="text-white mt-8 grid md:grid-cols-4 gap-4 ")= data.overview
        p(class="text-white mt-8 grid md:grid-cols-4 gap-4 ")= data.release_date
        
